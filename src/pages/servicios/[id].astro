---
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const details = await getCollection("details");

  return details.map((entry) => ({
    params: { id: entry.id },
    props: { servicio: entry.data },
  }));
}

const { servicio } = Astro.props;
---
