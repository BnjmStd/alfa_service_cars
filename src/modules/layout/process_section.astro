<section id="proceso" class="section">
  <div class="container">
    <div class="section-header reveal">
      <h2 style="font-size: 3rem;">
        Nuestro <span class="text-accent">Proceso</span>
      </h2>
      <p style="color: var(--color-text-muted);">
        Calidad artesanal con tecnología de punta.
      </p>
    </div>

    <div class="steps-container">
      <div class="step-item reveal">
        <div class="step-num">01</div>
        <div class="step-text">
          <h4>Descontaminado Profundo</h4>
          <p>
            Eliminamos impurezas férricas y minerales incrustados en los poros
            de la laca mediante descontaminación química y mecánica (Clay Bar).
          </p>
        </div>
      </div>
      <div class="step-item reveal">
        <div class="step-num">02</div>
        <div class="step-text">
          <h4>Corrección de Pintura</h4>
          <p>
            Pulido en múltiples etapas para eliminar micro-rayas (swirls) y
            devolver la profundidad original al color del vehículo.
          </p>
        </div>
      </div>
      <div class="step-item reveal">
        <div class="step-num">03</div>
        <div class="step-text">
          <h4>Blindaje Cerámico</h4>
          <p>
            Aplicación de recubrimientos de SiO2 que crean una capa rígida,
            ultra-brillante y repelente a líquidos sobre la superficie.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* --- PASOS DEL PROCESO --- */
  .steps-container {
    position: relative;
    margin-top: 40px;
  }
  .step-item {
    display: flex;
    gap: 30px;
    margin-bottom: 60px;
    opacity: 0;
    transform: translateY(30px);
    transition: 0.8s ease-out;
  }
  .step-item.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .step-num {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-accent);
    min-width: 40px;
  }
  .step-text h4 {
    font-size: 1.5rem;
    margin-bottom: 10px;
  }
  .step-text p {
    color: var(--color-text-muted);
  }
</style>
