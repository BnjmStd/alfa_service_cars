<section id="servicios" class="section">
  <div class="container">
    <h2 class="section-title scan-observe">
      Nuestros <span class="text-gradient">Servicios</span>
    </h2>

    <div class="grid-bento">
      <!-- CATEGORÍA PRINCIPAL -->
      <a class="bento-card card-large" href="/servicios/detailing-proteccion">
        <div class="card-media">
          <img src="/car1.jpeg" alt="Detailing y protección vehicular" />
          <video src="/a.mp4" muted loop playsinline></video>
        </div>

        <div class="card-content">
          <h3 class="card-title">Detailing & Protección de Pintura</h3>
          <p class="card-desc">
            Pulido de carrocería, tratamientos sintético, acrílico y cerámico.
            Recupera brillo, protege la pintura y prolonga la vida estética de
            tu vehículo.
          </p>
        </div>
      </a>

      <!-- CATEGORÍA VIDRIOS -->
      <a class="bento-card" href="/servicios/vidrios-seguridad">
        <div class="card-media">
          <img src="/vidrios.jpg" alt="Vidrios y seguridad vehicular" />
          <video src="/vidrios.mp4" muted loop playsinline></video>
        </div>

        <div class="card-content">
          <h3 class="card-title">Vidrios & Seguridad</h3>
          <p class="card-desc">
            Reparación de piquetes y trizaduras, polarizado certificado y
            láminas de seguridad para protección, normativa y tranquilidad al
            conducir.
          </p>
        </div>
      </a>

      <!-- CATEGORÍA LIMPIEZA -->
      <a class="bento-card" href="/servicios/limpieza-tecnica">
        <div class="card-media">
          <img src="/interior.jpg" alt="Limpieza técnica interior" />
          <video src="/interior.mp4" muted loop playsinline></video>
        </div>

        <div class="card-content">
          <h3 class="card-title">Limpieza Técnica & Sanitización</h3>
          <p class="card-desc">
            Descontaminación con ozono, limpieza interior profunda y
            sanitización del habitáculo para un ambiente más limpio y saludable.
          </p>
        </div>
      </a>

      <!-- CATEGORÍA TÉCNICA -->
      <a class="bento-card" href="/servicios/servicios-tecnicos">
        <div class="card-media">
          <img src="/motor.jpg" alt="Servicios técnicos vehiculares" />
          <video src="/motor.mp4" muted loop playsinline></video>
        </div>

        <div class="card-content">
          <h3 class="card-title">Servicios Técnicos</h3>
          <p class="card-desc">
            Lavado de motor profesional, limpieza controlada y mantención
            estética técnica sin dañar sensores ni componentes eléctricos.
          </p>
        </div>
      </a>

      <!-- CATEGORÍA MOTOS -->
      <a class="bento-card" href="/servicios/motos">
        <div class="card-media">
          <img src="/moto.jpg" alt="Detailing para motos" />
          <video src="/moto.mp4" muted loop playsinline></video>
        </div>

        <div class="card-content">
          <h3 class="card-title">Detailing para Motos</h3>
          <p class="card-desc">
            Servicios Elite Wash, tratamientos acrílicos y coatings cerámicos
            para proteger, embellecer y mantener tu moto en alto nivel.
          </p>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  /* -- Tarjetas Servicios (Bento) -- */
  .bento-card {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur));
    border-radius: var(--border-radius-lg);
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    min-height: 320px;
    position: relative;
    overflow: hidden;
    transition:
      transform 0.4s ease,
      border-color 0.4s ease;
  }

  .bento-card:hover {
    transform: translateY(-5px) scale(1.01);
    border-color: rgba(255, 255, 255, 0.2);

    cursor: pointer;
  }

  /* Luz roja ambiental en hover */
  .bento-card::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
      800px circle at var(--mouse-x, center) var(--mouse-y, center),
      var(--glass-bg),
      transparent 40%
    );
    opacity: 0;
    transition: opacity 0.5s;
    z-index: 1;
    pointer-events: none;
  }

  .bento-card:hover::after {
    background: radial-gradient(
      circle at top right,
      var(--color-accent-glow),
      transparent 60%
    );
    opacity: 0.4;
  }

  .card-content {
    position: relative;
    z-index: 2;
  }
  .card-title {
    font-size: 1.8rem;
    margin-bottom: 10px;
  }
  .card-desc {
    color: var(--color-text-muted);
  }
</style>
<style>
  /* --- Bento Media Hover (IMG → VIDEO) --- */
  .card-media {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
  }

  .card-media img,
  .card-media video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;

    filter: grayscale(60%);
  }

  /* Estado inicial */
  .card-media video {
    opacity: 0;
    transform: scale(1.05);
  }

  /* Hover: aparece video */
  .bento-card:hover .card-media video {
    opacity: 1;
    transform: scale(1);
  }

  .bento-card:hover .card-media img {
    opacity: 0;
  }

  /* Asegurar capas correctas */
  .bento-card {
    isolation: isolate;
  }

  .card-media {
    z-index: 0;
  }

  .bento-card::after {
    z-index: 1;
  }

  .card-content {
    z-index: 2;
  }
</style>

<script>
  document.querySelectorAll(".bento-card").forEach((card) => {
    const video = card.querySelector("video");
    if (!video) return;

    card.addEventListener("mouseenter", () => {
      video.currentTime = 0;
      video.play();
    });

    card.addEventListener("mouseleave", () => {
      video.pause();
    });
  });
</script>
