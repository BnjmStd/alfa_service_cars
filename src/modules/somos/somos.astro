---
import { Image } from "astro:assets";
import { getCollection } from "astro:content";

const todasLasFotos = await getCollection("somos");

const fotoHero = todasLasFotos.find((img) => img.id === "taller");
const fotoEquipo = todasLasFotos.find((img) => img.id === "team");

const pageContent = {
  hero: {
    title: "Pasión por el Detalle,",
    highlight: "Compromiso con tu Seguridad",
    subtitle:
      "En Alfa Detailing Talca elevamos el estándar del cuidado automotriz, combinando estética premium con soluciones de seguridad técnica.",
  },
  story: {
    badge: "Sobre Nosotros",
    title: "Más que estética,",
    gradient: "ingeniería aplicada a tu vehículo",
    paragraphs: [
      "En Alfa Detailing entendemos que un vehículo es más que un medio de transporte; es una inversión. Nacimos en Talca con una misión clara: profesionalizar el servicio de estética y seguridad automotriz.",
      "Nos diferenciamos por dejar atrás los métodos invasivos o caseros. Ya sea una limpieza de motor, una reparación de trizadura o un tratamiento cerámico, cada servicio es ejecutado con equipamiento específico y bajo estándares controlados.",
      "Somos expertos locales atendiendo a particulares y empresas que buscan un resultado superior, respaldado por certificaciones y garantías reales.",
    ],
  },
  values: [
    {
      title: "Técnica vs. Improvisación",
      icon: "🛠️",
      desc: "No lavamos autos, aplicamos procesos...",
    },
    {
      title: "Seguridad Certificada",
      icon: "🛡️",
      desc: "Cumplimos estrictamente la normativa chilena...",
    },
    {
      title: "Honestidad ante todo",
      icon: "🤝",
      desc: "Si un parabrisas se puede reparar, lo haremos...",
    },
    {
      title: "Tecnología Avanzada",
      icon: "🔬",
      desc: "Usamos Ozono molecular...",
    },
  ],
  cta: {
    title: "¿Listo para renovar tu vehículo?",
    desc: "Visítanos en Talca para una evaluación técnica gratuita. Elige calidad, elige seguridad.",
    primaryBtn: "Agendar Evaluación",
    secondaryBtn: "Ver Servicios",
  },
};
---

<section class="about-container">
  <div class="hero-section">
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <h1>
        {pageContent.hero.title}
        <span class="highlight">{pageContent.hero.highlight}</span>
      </h1>
      <p class="hero-subtitle">{pageContent.hero.subtitle}</p>
    </div>

    {
      fotoHero && (
        <Image
          src={fotoHero.data.foto}
          alt={fotoHero.data.description}
          class="hero-bg-img"
        />
      )
    }
  </div>

  <div class="story-section wrapper">
    <div class="story-text">
      <span class="badge">{pageContent.story.badge}</span>
      <h2>
        {pageContent.story.title}
        <br />
        <span class="text-gradient">{pageContent.story.gradient}</span>
      </h2>

      <div class="text-block">
        {pageContent.story.paragraphs.map((p) => <p>{p}</p>)}
      </div>
    </div>

    <div class="story-image-container">
      {
        fotoEquipo && (
          <Image src={fotoEquipo.data.foto} alt={fotoEquipo.data.description} />
        )
      }
      <div class="glow-effect"></div>
    </div>
  </div>

  <div class="values-section">
    <div class="wrapper">
      <div class="section-header center">
        <h2>Por qué elegir <span class="highlight">Alfa Detailing</span></h2>
        <p>Nuestros pilares fundamentales de trabajo</p>
      </div>

      <div class="values-grid">
        {
          pageContent.values.map((item) => (
            <div class="value-card">
              <div class="icon-wrapper">{item.icon}</div>
              <h4>{item.title}</h4>
              <p>{item.desc}</p>
            </div>
          ))
        }
      </div>
    </div>
  </div>

  <div class="visit-section">
    <div class="wrapper visit-grid">
      <div class="visit-info-grid">
        <div>
          <span class="badge">Te esperamos</span>
          <h2>Ven a <span class="highlight">Visitarnos</span></h2>
          <p>
            Estamos en el corazón de Talca listos para evaluar tu vehículo con
            estándares profesionales y atención personalizada.
          </p>

          <ul class="visit-list">
            <li><strong>📍 Dirección:</strong> Av. Ejemplo 1234, Talca</li>
            <li>
              <strong>⏰ Horario:</strong> Lunes a Viernes · 09:00 – 18:30
            </li>
            <li><strong>📞 Teléfono:</strong> +56 9 1234 5678</li>
          </ul>
        </div>

        <div>
          {
            fotoHero && (
              <Image
                src={fotoHero.data.foto}
                alt={fotoHero.data.description}
                class=""
              />
            )
          }
        </div>

        <a
          href="https://www.google.com/maps/search/?api=1&query=Talca+Chile"
          target="_blank"
          class="btn btn-primary"
        >
          Cómo llegar
        </a>
      </div>
    </div>
  </div>

  <div class="cta-section wrapper center">
    <div class="cta-card">
      <h2>{pageContent.cta.title}</h2>
      <p>{pageContent.cta.desc}</p>

      <div class="cta-buttons">
        <a href="/contacto" class="btn btn-primary"
          >{pageContent.cta.primaryBtn}</a
        >
        <a href="/servicios" class="btn btn-outline"
          >{pageContent.cta.secondaryBtn}</a
        >
      </div>
    </div>
  </div>
</section>
<style>
  /* --- TU TEMA DEFINIDO --- */
  :root {
    --color-bg-main: #000000;
    --color-bg-surface: #121212;
    --color-text-main: #ffffff;
    --color-text-muted: #a1a1a6;

    --color-accent: #d90429;
    --color-accent-glow: rgba(217, 4, 41, 0.5);

    --glass-bg: rgba(255, 255, 255, 0.05);
    --glass-border: rgba(255, 255, 255, 0.1);
    --glass-blur: 20px;

    --font-primary: "Inter", sans-serif;
    --border-radius-lg: 24px;
  }

  /* --- ESTILOS GENERALES --- */
  .about-container {
    font-family: var(--font-primary);
    color: var(--color-text-main);
    background-color: var(--color-bg-main);
    line-height: 1.6;
    overflow-x: hidden;
  }

  .wrapper {
    max-width: 1200px;
    margin: 0 auto;
    padding: 5rem 1.5rem;
  }

  .center {
    text-align: center;
  }

  h1,
  h2,
  h4 {
    color: var(--color-text-main);
    line-height: 1.2;
    font-weight: 700;
  }

  h2 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
  }
  p {
    color: var(--color-text-muted);
  }

  strong {
    color: var(--color-text-main);
    font-weight: 600;
  }

  /* Texto resaltado en ROJO */
  .highlight {
    color: var(--color-accent);
    text-shadow: 0 0 20px var(--color-accent-glow);
  }

  /* --- 1. HERO SECTION --- */
  .hero-section {
    position: relative;
    height: 100vh;
    min-height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: var(--color-bg-main);
    text-align: center;
    padding: 0 1rem;
  }

  .hero-bg-img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.6;
    z-index: 0;
    object-position: center top;
    /* FADE A NEGRO ABAJO */
    -webkit-mask-image: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 1) 65%,
      rgba(0, 0, 0, 0) 100%
    );
    mask-image: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 1) 65%,
      rgba(0, 0, 0, 0) 100%
    );
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* Degradado para que el texto se lea bien sobre el negro */
    background: linear-gradient(to top, var(--color-bg-main), transparent);
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .hero-content h1 {
    font-size: clamp(2.5rem, 5vw, 4rem); /* Responsive typography */
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: #e5e5e5; /* Un poco más blanco que el muted para leer mejor */
  }

  /* --- 2. STORY SECTION --- */
  .story-section {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
  }

  @media (min-width: 900px) {
    .story-section {
      grid-template-columns: 1fr 1fr;
    }
  }

  .badge {
    display: inline-block;
    color: var(--color-accent);
    background: rgba(217, 4, 41, 0.1); /* Rojo muy suave de fondo */
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.75rem;
    margin-bottom: 1rem;
    border: 1px solid var(--color-accent-glow);
  }

  .text-block p {
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
  }

  /* Contenedor imagen con estilo */
  .story-image-container {
    position: relative;
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    border: 1px solid var(--glass-border);
  }

  .story-image-container img {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.7s ease;
  }

  .story-image-container:hover img {
    transform: scale(1.05);
  }

  /* Resplandor rojo decorativo detrás de la imagen */
  .glow-effect {
    position: absolute;
    top: -50px;
    right: -50px;
    width: 150px;
    height: 150px;
    background: var(--color-accent);
    filter: blur(80px);
    opacity: 0.4;
    z-index: 1;
    pointer-events: none;
  }

  /* --- 3. VALUES SECTION (Glassmorphism) --- */
  .values-section {
    background-color: var(
      --color-bg-surface
    ); /* Fondo un poco más claro que el negro puro */
  }

  .values-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 640px) {
    .values-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (min-width: 1024px) {
    .values-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .value-card {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)); /* Efecto borroso detrás */
    padding: 2rem;
    border-radius: var(--border-radius-lg);
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .value-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.5);
    border-color: var(--color-accent-glow); /* Borde brilla rojo al hover */
  }

  .icon-wrapper {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    /* Filtro para que los emojis se vean un poco más integrados si quieres */
    filter: grayscale(0.2);
  }

  .value-card h4 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
    color: var(--color-white);
  }
  .value-card p {
    font-size: 0.95rem;
  }

  /* --- 4. CTA SECTION --- */
  .cta-section {
    position: relative;
  }

  .cta-card {
    background: linear-gradient(
      135deg,
      var(--color-bg-surface),
      var(--color-bg-main)
    );
    border: 1px solid var(--glass-border);
    border-radius: var(--border-radius-lg);
    padding: 4rem 2rem;
    position: relative;
    overflow: hidden;
  }

  /* Decoración roja sutil en el CTA */
  .cta-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 5px;
    background: var(--color-accent);
    box-shadow: 0 0 20px 5px var(--color-accent-glow);
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
  }

  /* BOTONES */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 1rem;
  }

  .btn-primary {
    background-color: var(--color-accent);
    color: var(--color-text-main);
    box-shadow: 0 0 20px var(--color-accent-glow);
    border: 1px solid transparent;
  }

  .btn-primary:hover {
    background-color: #b00320; /* Un rojo un poco más oscuro */
    transform: scale(1.05);
    box-shadow: 0 0 30px var(--color-accent);
  }

  .btn-outline {
    background-color: transparent;
    border: 1px solid var(--glass-border);
    color: var(--color-text-main);
  }

  .btn-outline:hover {
    background-color: var(--glass-bg);
    border-color: var(--color-text-main);
  }
</style>

<style>
  /* --- 5. VISIT SECTION --- */
  .visit-section {
    background: linear-gradient(
      to bottom,
      var(--color-bg-main),
      var(--color-bg-surface)
    );
    border-top: 1px solid var(--glass-border);
  }

  .visit-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 900px) {
    .visit-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .visit-info p {
    max-width: 480px;
    margin-bottom: 2rem;
    display: flex;
  }

  .visit-list {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
  }

  .visit-list li {
    margin-bottom: 0.75rem;
    color: var(--color-text-muted);
    font-size: 1rem;
  }

  .visit-map {
    position: relative;
    width: 100%;
    height: 320px;
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    border: 1px solid var(--glass-border);
  }

  .visit-map iframe {
    width: 100%;
    height: 100%;
    border: 0;
    filter: grayscale(1) contrast(1.1) brightness(0.8);
  }

  .visit-info-grid {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  @media (min-width: 900px) {
    .visit-info-grid {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  /* Texto */
  .visit-info-grid > div:first-child {
    flex: 1;
    max-width: 520px;
  }

  /* Imagen */
  .visit-info-grid img {
    width: 100%;
    max-width: 520px;
    border-radius: var(--border-radius-lg);
    border: 1px solid var(--glass-border);
    object-fit: cover;
  }

  /* Botón alineado */
  .visit-info-grid > a {
    align-self: flex-start;
  }

  @media (min-width: 900px) {
    .visit-info-grid > a {
      align-self: flex-end;
    }
  }
  .visit-info-grid {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  @media (min-width: 900px) {
    .visit-info-grid {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  /* Texto */
  .visit-info-grid > div:first-child {
    flex: 1;
    max-width: 520px;
  }

  /* Imagen */
  .visit-info-grid img {
    width: 100%;
    max-width: 520px;
    border-radius: var(--border-radius-lg);
    border: 1px solid var(--glass-border);
    object-fit: cover;
  }

  /* Botón alineado */
  .visit-info-grid > a {
    align-self: flex-start;
  }

  @media (min-width: 900px) {
    .visit-info-grid > a {
      align-self: flex-end;
    }
  }
</style>
