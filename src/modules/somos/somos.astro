---
const values = [
  {
    title: "Técnica vs. Improvisación",
    desc: "No lavamos autos, aplicamos procesos. Desde el cuidado de sensores en el motor hasta la inyección de resinas, todo sigue un protocolo técnico riguroso.",
    icon: "🛠️"
  },
  {
    title: "Seguridad Certificada",
    desc: "Cumplimos estrictamente la normativa chilena en polarizados y láminas de seguridad. Tu tranquilidad ante fiscalizaciones es nuestra prioridad.",
    icon: "🛡️"
  },
  {
    title: "Honestidad ante todo",
    desc: "Si un parabrisas se puede reparar, lo haremos. Asesoramos según lo que tu vehículo realmente necesita, cuidando tu bolsillo y la originalidad del auto.",
    icon: "🤝"
  },
  {
    title: "Tecnología Avanzada",
    desc: "Usamos Ozono molecular, máquinas rotativas de precisión y selladores cerámicos de última generación. Invertimos en las mejores herramientas.",
    icon: "🔬"
  }
];
---

<section class="about-container">
  
  <div class="hero-section">
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <h1>Pasión por el Detalle, <span class="highlight">Compromiso con tu Seguridad</span></h1>
      <p class="hero-subtitle">
        En Alfa Detailing Talca elevamos el estándar del cuidado automotriz, combinando estética premium con soluciones de seguridad técnica.
      </p>
    </div>
    <img src="/images/taller-hero.webp" alt="Taller Alfa Detailing" class="hero-bg-img" />
  </div>

  <div class="story-section wrapper">
    <div class="story-text">
      <span class="badge">Sobre Nosotros</span>
      <h2>Más que estética, <br> <span class="text-gradient">ingeniería aplicada a tu vehículo</span></h2>
      
      <div class="text-block">
        <p>
          En <strong>Alfa Detailing</strong> entendemos que un vehículo es más que un medio de transporte; es una inversión. Nacimos en <strong>Talca</strong> con una misión clara: profesionalizar el servicio de estética y seguridad automotriz.
        </p>
        <p>
          Nos diferenciamos por dejar atrás los métodos invasivos o caseros. Ya sea una limpieza de motor, una reparación de trizadura o un tratamiento cerámico, cada servicio es ejecutado con <strong>equipamiento específico y bajo estándares controlados</strong>.
        </p>
        <p>
            Somos expertos locales atendiendo a particulares y empresas que buscan un resultado superior, respaldado por certificaciones y garantías reales.
        </p>
      </div>
    </div>
    
    <div class="story-image-container">
      <img src="/images/equipo-trabajando.webp" alt="Equipo Alfa Detailing trabajando" />
      <div class="glow-effect"></div>
    </div>
  </div>

  <div class="values-section">
    <div class="wrapper">
      <div class="section-header center">
        <h2>Por qué elegir <span class="highlight">Alfa Detailing</span></h2>
        <p>Nuestros pilares fundamentales de trabajo</p>
      </div>

      <div class="values-grid">
        {values.map((item) => (
          <div class="value-card">
            <div class="icon-wrapper">{item.icon}</div>
            <h4>{item.title}</h4>
            <p>{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </div>

  <div class="cta-section wrapper center">
    <div class="cta-card">
      <h2>¿Listo para renovar tu vehículo?</h2>
      <p>Visítanos en Talca para una evaluación técnica gratuita. Elige calidad, elige seguridad.</p>
      
      <div class="cta-buttons">
        <a href="/contacto" class="btn btn-primary">Agendar Evaluación</a>
        <a href="/servicios" class="btn btn-outline">Ver Servicios</a>
      </div>
    </div>
  </div>

</section>

<style>
  /* --- TU TEMA DEFINIDO --- */
  :root {
    --color-bg-main: #000000;
    --color-bg-surface: #121212;
    --color-text-main: #ffffff;
    --color-text-muted: #a1a1a6;

    --color-accent: #d90429;
    --color-accent-glow: rgba(217, 4, 41, 0.5);

    --glass-bg: rgba(255, 255, 255, 0.05);
    --glass-border: rgba(255, 255, 255, 0.1);
    --glass-blur: 20px;

    --font-primary: "Inter", sans-serif;
    --border-radius-lg: 24px;
  }

  /* --- ESTILOS GENERALES --- */
  .about-container {
    font-family: var(--font-primary);
    color: var(--color-text-main);
    background-color: var(--color-bg-main);
    line-height: 1.6;
    overflow-x: hidden; /* Evita scroll horizontal indeseado */
  }

  .wrapper {
    max-width: 1200px;
    margin: 0 auto;
    padding: 5rem 1.5rem;
  }

  .center { text-align: center; }

  h1, h2, h4 {
    color: var(--color-text-main);
    line-height: 1.2;
    font-weight: 700;
  }

  h2 { font-size: 2.5rem; margin-bottom: 1.5rem; }
  p { color: var(--color-text-muted); }

  strong { color: var(--color-text-main); font-weight: 600; }

  /* Texto resaltado en ROJO */
  .highlight {
    color: var(--color-accent);
    text-shadow: 0 0 20px var(--color-accent-glow);
  }

  /* --- 1. HERO SECTION --- */
  .hero-section {
    position: relative;
    height: 50vh;
    min-height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: var(--color-bg-main);
    text-align: center;
    padding: 0 1rem;
  }

  .hero-bg-img {
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    opacity: 0.6;
    z-index: 0;
  }

  .hero-overlay {
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    /* Degradado para que el texto se lea bien sobre el negro */
    background: linear-gradient(to top, var(--color-bg-main), transparent);
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .hero-content h1 {
    font-size: clamp(2.5rem, 5vw, 4rem); /* Responsive typography */
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: #e5e5e5; /* Un poco más blanco que el muted para leer mejor */
  }

  /* --- 2. STORY SECTION --- */
  .story-section {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
  }

  @media (min-width: 900px) {
    .story-section { grid-template-columns: 1fr 1fr; }
  }

  .badge {
    display: inline-block;
    color: var(--color-accent);
    background: rgba(217, 4, 41, 0.1); /* Rojo muy suave de fondo */
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.75rem;
    margin-bottom: 1rem;
    border: 1px solid var(--color-accent-glow);
  }

  .text-block p { margin-bottom: 1.5rem; font-size: 1.1rem; }

  /* Contenedor imagen con estilo */
  .story-image-container {
    position: relative;
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    border: 1px solid var(--glass-border);
  }

  .story-image-container img {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.7s ease;
  }

  .story-image-container:hover img { transform: scale(1.05); }

  /* Resplandor rojo decorativo detrás de la imagen */
  .glow-effect {
    position: absolute;
    top: -50px; right: -50px;
    width: 150px; height: 150px;
    background: var(--color-accent);
    filter: blur(80px);
    opacity: 0.4;
    z-index: 1;
    pointer-events: none;
  }

  /* --- 3. VALUES SECTION (Glassmorphism) --- */
  .values-section {
    background-color: var(--color-bg-surface); /* Fondo un poco más claro que el negro puro */
  }

  .values-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 640px) { .values-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (min-width: 1024px) { .values-grid { grid-template-columns: repeat(4, 1fr); } }

  .value-card {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)); /* Efecto borroso detrás */
    padding: 2rem;
    border-radius: var(--border-radius-lg);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .value-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5);
    border-color: var(--color-accent-glow); /* Borde brilla rojo al hover */
  }

  .icon-wrapper {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    /* Filtro para que los emojis se vean un poco más integrados si quieres */
    filter: grayscale(0.2); 
  }

  .value-card h4 { font-size: 1.25rem; margin-bottom: 0.75rem; color: var(--color-white); }
  .value-card p { font-size: 0.95rem; }

  /* --- 4. CTA SECTION --- */
  .cta-section {
    position: relative;
  }
  
  .cta-card {
    background: linear-gradient(135deg, var(--color-bg-surface), var(--color-bg-main));
    border: 1px solid var(--glass-border);
    border-radius: var(--border-radius-lg);
    padding: 4rem 2rem;
    position: relative;
    overflow: hidden;
  }

  /* Decoración roja sutil en el CTA */
  .cta-card::before {
    content: '';
    position: absolute;
    top: 0; left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 5px;
    background: var(--color-accent);
    box-shadow: 0 0 20px 5px var(--color-accent-glow);
  }

  .cta-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
  }

  /* BOTONES */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 1rem;
  }

  .btn-primary {
    background-color: var(--color-accent);
    color: var(--color-text-main);
    box-shadow: 0 0 20px var(--color-accent-glow);
    border: 1px solid transparent;
  }

  .btn-primary:hover {
    background-color: #b00320; /* Un rojo un poco más oscuro */
    transform: scale(1.05);
    box-shadow: 0 0 30px var(--color-accent);
  }

  .btn-outline {
    background-color: transparent;
    border: 1px solid var(--glass-border);
    color: var(--color-text-main);
  }

  .btn-outline:hover {
    background-color: var(--glass-bg);
    border-color: var(--color-text-main);
  }

</style>